language: csharp
os: windows
mono: none
dotnet: 3.1.302
before_install:
 - sudo apt-get update
install:
 - dotnet restore
script:
 - dotnet build ./MintPlayer.BrowserDialog/MintPlayer.BrowserDialog.csproj --configuration Release
sudo: required